bootJar {
    archivesBaseName = 'nr_admin'
}
jar {
    enabled = true
}

dependencies {
    implementation 'de.codecentric:spring-boot-admin-starter-server'
    implementation 'org.springframework.boot:spring-boot-starter-security'

}