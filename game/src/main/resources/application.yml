spring:
  profiles:
    group:
      local:
        - "local-core-cache-redis"
        - "local-common"
        - "local-domain-db"
        - "local-domain-redis"
        - "local-game"
      dev:
        - "dev-core-cache-redis"
        - "dev-common"
        - "dev-domain-db"
        - "dev-domain-redis"
        - "dev-game"

    default: local
